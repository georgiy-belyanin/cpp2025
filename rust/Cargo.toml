[package]
name = "cpp2025"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.39", features = ["derive"] }
num_cpus = "1.16.0"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
criterion = "0.5.0"

[[bench]]
name = "benchmarks"
harness = false
